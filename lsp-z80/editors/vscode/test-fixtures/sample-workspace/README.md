# Sample Z80 workspace

Open `main.asm` with the extension installed and a configured `lsp-z80` server.
The `2Ah` literal intentionally produces `pasmo.non-standard-hex`; changing it
to `$2a` should clear that warning.
